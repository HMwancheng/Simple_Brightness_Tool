<Window x:Class="BrightnessWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HM's Simple Brightness Tool"
        Width="400"
        SizeToContent="Height"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="Manual"
        Background="{StaticResource BackgroundBrush}"
        Closing="Window_Closing"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 头部 -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="控制中心" FontSize="16" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- 占位：后续逐步移植显示器列表 -->
        <StackPanel Grid.Row="1" Margin="16,24" HorizontalAlignment="Center">
            <TextBlock Text="WPF UI 壳已就绪" FontSize="14"
                       Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="亮度功能将在后续步骤逐步移植到这里"
                       FontSize="11" Margin="0,8,0,0"
                       Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>